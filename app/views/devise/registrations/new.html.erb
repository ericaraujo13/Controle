<% content_for(:title) { 'Cadastre-se' } %>

<%= form_with model: resource,
              scope: resource_name,
              url: user_registration_path do |f| %>
  <div>
    <%= f.label :username, class: 'form-label' %>
    <%= f.text_field :username, class: 'form-input', required: true %>
  </div>
  <div>
    <%= f.label :email, class: 'form-label' %>
    <%= f.email_field :email, class: 'form-input', required: true %>
  </div>
  <div>
    <%= f.label :password, class: 'form-label' %>
    <%= f.password_field :password, class: 'form-input', required: true %>
  </div>
  <div>
    <%= f.label :password_confirmation, class: 'form-label' %>
    <%= f.password_field :password_confirmation, class: 'form-input', required: true %>
  </div>
  <%= f.submit 'Cadastrar', class: 'w-full button button-primary' %>
<% end %>
